# Exhaustive matter-even D-flat cone — corrected proof gate

The raw class-wide inputs remain absent from Git: sp1/*.sp, sp2/*.sp, and unbroken2.py. Therefore the 88-model exact parity-cone problem cannot presently be regenerated.

A second correction is now frozen. The old fixed-support routine described its 0/24 result as an exact failure to make a D-flat support matter-even, but the source actually solved

3(B-L)_s = 0

for every support singlet. That preserves the full continuous U(1). Matter parity requires only

3(B-L)_s in 2 Z.

Accordingly, the 0/24 survives only as a full-U(1) neutrality result and is withdrawn as exact Z2 evidence.

What still stands is the sampled result: 88 models with a B-L direction, 60 sampled choices per model, zero matter-even D-flat hits, with the sampled source explicitly checking integrality and evenness. That is controlled evidence, not a proof.

The exact closure contract is unchanged but now correctly motivated: solve x=x0+Nt over Q, enumerate every realizable parity/integrality class of 3x.Q_s, and for each class solve the D-flat cone on its even singlets. Every infeasible class needs an exact Farkas witness; any exact primal ray is a counterexample.
