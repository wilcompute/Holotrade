# Exhaustive matter-even D-flat cone — execution result

The fifth requested attack was executed as a **proof-provenance audit** rather
than being forced into a false theorem.

The current Holotrade Git tree does not contain the raw inputs used by the
class-wide matter-parity and D-flat scripts:

- `sp1/*.sp`: absent;
- `sp2/*.sp`: absent;
- `unbroken2.py`: absent.

A repository-history search also found no commit carrying those paths.  The
current summaries therefore cannot be independently expanded back into all
per-field rational charge matrices.

What *is* frozen remains strong evidence:

[
88 	ext{models with a }B-L	ext{ direction},
]
with nullspace dimensions
[
2^{18},3^{30},4^{34},5^3,6^3.
]
The current scan tested 60 (B-L) choices per model and found zero D-flat
cones on matter-even singlets; on 24 found D-flat supports, an exact linear
shift making the complete support even succeeded zero times.  The owning
certificate explicitly marks this as **not a proof** because the (B-L)
freedom was sampled.

## Exact closure contract

The new gate specifies the normalized ledger needed to finish this without
sampling: exact rational U(1) charges for every left-chiral field, labels and
nonabelian metadata, anomalous direction/FI sign, hypercharge data, the
(q,u^c,d^c,e^c) (B-L) constraints, and the complete singlet set.

For each model the future exhaustive producer must solve
[
x=x_0+Nt
]
over (mathbb Q), enumerate every realizable parity/integrality class of
[
3(xcdot Q_s),
]
and solve the even-singlet D-flat cone exactly.  Every infeasible class must
carry a Farkas witness
[
A^T yge0,qquad b^T y<0.
]
Any exact feasible primal ray is instead a counterexample.

Thus target five is now regression-protected against accidental promotion of
the 60-sample zero into a theorem.  The committed flagship weight dump remains
useful for its own one-model FI/Cartan audits but does not substitute for the
missing 88-model (B-L) ledger.
