# Local nine-weight completion and a conditional splitting obstruction

September 20, 2026. Reproduce with `python3 analysis/local_nine_spurion_algebra.py`; `--write` refreshes its JSON certificate.

The incoming retraction `retraction_the_local_gut_multiplet_is_complete_so_the_coefficients_are_locked.py` supplies the exact flagship shift and five displayed weights. It correctly withdraws the independent-coefficient interpretation and explicitly leaves local Clebsch factors and singlet representations uncomputed. This audit addresses one bounded part of that residual question; it does not replay its 604-point census or establish coefficient equality in the string model.

Enumerating all 480 E8 x E8 roots using integers scaled by six recovers 156 shift-invariant roots and six roots joining the triplet and doublet. Closing the displayed states under their exact Weyl reflections gives **nine**, not five, weights. All 72 ordered differences between distinct weights are local roots, giving an explicit A8 root subsystem. The four additional weights have zero pairing with the displayed color A2 and weak A1 simple roots. Neutrality under these roots alone does not establish hypercharge neutrality, a surviving four-dimensional field, or an allowed VEV.

This enables a precise conditional question. Suppose the available symmetry-breaking endomorphisms are generated by the identity and fundamental-antifundamental outer products supported on those four extra weight directions. In the ordered basis (triplet 3, doublet 2, extra 4), their full associative algebra is

**C I9 + M4(C)**, dimension **17**.

The 16 bottom-right matrix units multiply among themselves; adjoining the identity gives the displayed algebra. Independence follows from their disjoint entries and the identity's nonzero first block. Consequently every polynomial word restricts to a single scalar on the five displayed directions. This is an all-orders statement about this specified finite matrix algebra, not a no-go theorem for all allowed string couplings. Its commutant is M5(C) plus scalar I4, dimension 26: commuting with all bottom matrix units kills cross blocks and forces the bottom block scalar, while the top block remains arbitrary. The producer checks all 289 basis products and all basis/commutant pairs exactly.

An explicit escape target is the traceless diagonal endomorphism

**diag(4,4,4,0,0,-3,-3,-3,-3)**.

It commutes with the displayed color and weak generators, is nonzero on the triplet, vanishes on the doublet, and lies outside the 17-dimensional algebra. Thus that restricted spurion library cannot generate this target at any polynomial order. An adjoint-like source outside this library would be needed for this particular splitting mechanism. We have not shown that such a source occurs, survives orbifold projection, satisfies D/F-flatness, or has an allowed coupling. Computing actual singlet representations and invariant tensors is the next physical test.

The contribution is the exact replay, full nine-weight closure, and explicit conditional algebra/escape target. Classical matrix-algebra facts and the incoming local-multiplet observation are not claimed as new mathematics. The unrelated 27-state E6 and Pauli constructions are not identified by dimension with this nine-state orbit.
